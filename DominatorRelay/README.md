# DominatorRelay

This component was previously used as a central relay for check-ins. The project
has since been updated to communicate directly over TailScale, so this directory
is kept for historical reference only and is no longer required.
